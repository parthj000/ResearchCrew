from research_graph import ResearchGraph

kapa = ResearchGraph()
# initial_state = kapa.generate_initial_state(query="aids treatment")
# response = kapa.graph.invoke(initial_state)

# print(response)
